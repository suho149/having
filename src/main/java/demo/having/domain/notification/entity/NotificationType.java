package demo.having.domain.notification.entity;

public enum NotificationType {
    NEW_STUDY_CREATED, // 새로 추가
    NEW_MESSAGE,
    SCHEDULE_UPDATED,
    NEW_MEMBER_JOINED,
    STUDY_JOIN_REQUEST,
    // ... (기존 타입)
}
